# MOSFET Data Plotter — runtime dependencies.
# Use Python >= 3.10. matplotlib >= 3.8 is required so mathtext labels
# (subscripts/superscripts like $V_\mathrm{g}$, $10^{-9}$) render in the chosen
# body font (e.g. Graphik); older matplotlib cannot use an arbitrary TTF for math.
numpy>=1.24
scipy>=1.8         # Savitzky-Golay / median / Gaussian smoothing
matplotlib>=3.8
PySide6>=6.5       # Qt GUI (matplotlib's Qt backend needs Qt >= 5.10)
